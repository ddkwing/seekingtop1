services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: seekingtop1
      POSTGRES_PASSWORD: seekingtop1
      POSTGRES_DB: seekingtop1
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
